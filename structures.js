//Push Pop, Peek, lengh 

function testArray(str) {


    for (const key in str) {
        console.log(key);
    }

    for (const key in object) {
        if (Object.hasOwnProperty.call(object, key)) {
            const element = object[key];
            
        }
    }

    // for
}

console.log([1,2,3,4,5,7,6]);
